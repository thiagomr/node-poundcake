## Setup

Install dependencies:

```bash
$ yarn install
```

Start app:

```bash
$ yarn start
```

Run on development:

```bash
$ yarn start:dev
```

Run on Docker:

```bash
$ bash docker.sh start
```

Run tests:

```bash
$ yarn test
```

Run linter:

```bash
$ yarn lint
```
